import 'dart:io';
import 'Data/libraryData.dart';
import 'Classes/libraryClass.dart';

void main() {
  // var lib = Edits().modifyBookData(userIndex: 1, title: 'title', newTitle: newTitle, author: author, categories: categories, copies: copies, price: price, publishDate: publishDate);
  // print(lib);
}
